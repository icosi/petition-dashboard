# React + Vite

How to run:
- `git clone https://github.com/icosi/petition-dashboard` and enter project folder
- `npm install` to download dependencies
- `npm run dev` to run app
- Go to http://localhost:3000 to view the dashboards